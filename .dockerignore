# git + CI
.git
.github
.gitignore

# deps - reinstalled fresh in the build
node_modules
**/node_modules

# mutable state (lives on the /data volume, never baked into the image).
# Note: the *.db* patterns drop the database FILES; we must NOT exclude the
# server/db/ directory itself - it holds database.js + schema.sql.
*.db
*.db-wal
*.db-shm
*.db.*
server/uploads
server/certs

# secrets
.env
**/.env

# not needed at runtime (NOTE: scripts/ IS needed - database.js requires
# scripts/migrate-multitenancy at boot - so it must stay in the image)
server/test
# Studio 6.3 docker verify needs these suites inside the image:
!server/test/studio-export.test.js
!server/test/studio-spike.test.js
!server/test/suika-spike.test.js
!server/test/suika-export.test.js
!server/test/content-404.test.js
!server/test/brand-kit.test.js
!server/test/slide-fonts.test.js
android
tizen
video
*.apk
*.wgt
*.tar.gz

# docs / editor cruft
*.md
!.dockerignore
.vscode
.idea
.DS_Store

# Suika monorepo: keep apps/suika + packages only in the build context (Phase 0 island).
frontend-studio_v3/apps/backend
frontend-studio_v3/apps/suika-multiplayer
frontend-studio_v3/apps/workbench
frontend-studio_v3/apps/docs
frontend-studio_v3/demo
frontend-studio_v3/.husky
frontend-studio_v3/.github
