# Isolated Studio 6.0 spike — not the main CMS compose stack.
services:
  studio-spike:
    build:
      context: ../..
      dockerfile: docker/studio-spike/Dockerfile
    volumes:
      - ./out:/out
    # One-shot verify; container exits 0 when PNG is 1920×1080 and licence-check passed.
