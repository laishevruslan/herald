"use client";

export const UserButton = () => {
  return (
    <button
      type="button"
      className="size-9 rounded-full bg-gradient-to-br from-gray-700 to-gray-900 text-white text-xs font-semibold flex items-center justify-center shadow-sm hover:opacity-90 transition"
      title="Account"
    >
      U
    </button>
  );
};

export default UserButton;
